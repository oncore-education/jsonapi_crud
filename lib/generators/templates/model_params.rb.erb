module JsonapiCrud
  module <%= @config[:classname] %>Params

    def base_route
      "<%= @config[:base_url] %>/<%= @config[:type] %>"
    end

    def model_type
      "<%= @config[:type] %>"
    end

    def model
        <%= @config[:classname] %>
    end

    def model_factory
      :<%= @config[:model] %>
    end

    # CREATE

    def create_params(id = nil)
      formatted_params({ }, id)
    end

    def expected_create_attributes
      [ ]
    end

    # SHOW
    def expected_show_attributes
      [ ]
    end

    # UPDATE

    def update_params(id = nil)
      formatted_params( { }, id )
    end

    def expected_update_attributes
      [ ]
    end

    # INVALID

    def invalid_params(id = nil)
      formatted_params({ }, id)
    end

    def unprocessable_parms(id = nil)
      [ ]
    end

  end
end
